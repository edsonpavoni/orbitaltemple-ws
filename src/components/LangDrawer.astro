---
import LanguageSwitcher from "./LanguageSwitcher.tsx";
const { id = "lang-drawer" } = Astro.props;
---
<aside id={id} class="drawer drawer--left" style="background-color: #fff; color: #553903;"
role="dialog" aria-modal="true" aria-labelledby="lang-title" data-open="false">
  <button style="position: absolute; top: 1.5rem; left: 2rem; padding: 0.5rem; background: none; border: none; cursor: pointer;" data-close aria-label="close">
    <img src="/close_icon.svg" alt="close" style="width: 1.8rem; height: 1.8rem;" />
  </button>

  <div class="menu-drawer-content" style="align-items: center !important; text-align: center !important;">
    {/* React component for language switching */}
    <LanguageSwitcher client:load />
  </div>
</aside>
