---
import MenuContent from "./MenuContent";
const { id = "menu-drawer" } = Astro.props;
---
<aside id={id} class="drawer drawer--right" style="background-color: #fff; color: #553903;"
role="dialog" aria-modal="true" aria-labelledby="menu-title" data-open="false">
  <button style="position: absolute; top: 1.5rem; right: 2rem; padding: 0.5rem; background: none; border: none; cursor: pointer;" data-close aria-label="close">
    <img src="/UI/close_icon.svg" alt="close" style="width: 1.8rem; height: 1.8rem;" />
  </button>

  <MenuContent client:only="react" />
</aside>